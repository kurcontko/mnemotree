# Legacy Main Repo Archive

This directory contains markdown files copied from the older `/home/qrc/repos/mnemotree` tree.

## Layout

- `docs/`: files that originally lived under the old repo's `docs/` directory
- `root/`: top-level markdown files from the old repo that were effectively part of its documentation set

## Purpose

These files were copied into the new workspace so the historical planning and research context remains available during cleanup work. They are archival reference material, not the active documentation set for the copied `mnemotree` repo.
